# Random forest: a strong, low-variance ensemble candidate.
run_name: rf
model:
  type: random_forest
  params:
    n_estimators: 300
    max_depth: null
    random_state: 42
    n_jobs: -1
data:
  dataset: digits
  test_size: 0.2
  val_size: 0.2
  random_state: 42
notes: "Bagged trees; strong accuracy with a modest generalisation gap."
